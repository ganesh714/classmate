/**
 * Global Configuration for ClassMate AI Application
 * 
 * Centralizing the backend URL allowing for easier switching between
 * development (local) and production (Render) environments.
 */
const CONFIG = {
    // Backend API Base URL
    // API_BASE_URL: "https://classmate-juji.onrender.com"
    API_BASE_URL: "http://localhost:8000"
};
